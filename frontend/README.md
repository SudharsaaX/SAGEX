# SAGE-X Frontend

Minimal React (Vite) frontend to interact with the Flask backend.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend to run at `http://127.0.0.1:5000`.
